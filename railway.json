{
  "build": {
    "builder": "NIXPACKS",
    "options": {
      "runtime": "python311"
    }
  },
  "deploy": {
    "startCommand": "uvicorn backend.main:app --host 0.0.0.0 --port $PORT",
    "healthCheckPath": "/api/health"
  }
}